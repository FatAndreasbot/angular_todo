import { Routes } from '@angular/router';
import { AllTasks } from './all-tasks/all-tasks';

export const routes: Routes = [
    {
        path:'',
        title:'all tasks',
        component: AllTasks
    }
];
